# TechHelp Solutions

Estrutura do projeto:

- `backend/`: API ASP.NET Core, controllers, services, models e configuracoes.
- `frontend/`: paginas HTML, scripts e demais arquivos da interface.

Arquivos de apoio:

- `UsuariosApi.slnx`: solucao na raiz apontando para `backend/UsuariosApi.csproj`.
- `.gitignore`: regras do repositório.
